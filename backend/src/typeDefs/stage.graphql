type Stage {
  id: ID!
  name: String!
  path: String!
  roadmapId: String!
}

type Query {
  stages: [Stage]
}

type Mutation {
  createStage(input: CreateStageInput): Stage
}

input CreateStageInput {
  name: String!
  path: String!
  newId: String!
  roadmapId: String!
}
